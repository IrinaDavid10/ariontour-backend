variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

stages:
  - build
  - test
  - sonarqube

build:
  stage: build
  script:
    - ./gradlew assemble

sonarqube:
  stage: sonarqube
  script:
    - ./gradlew sonarqube

test:
  stage: test
  script:
    - ./gradlew test

